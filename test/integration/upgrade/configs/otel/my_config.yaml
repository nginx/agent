receivers:
  hostmetrics:
    collection_interval: 10s
    scrapers:
      cpu:

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    metrics:
      receivers: [hostmetrics]
      exporters: [debug]