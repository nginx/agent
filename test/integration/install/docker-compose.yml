version: '3.9'

services:
  agent:
    build:
      context: ../../../
      dockerfile: ./test/docker/integration/install/Dockerfile
      args:
        PACKAGE_NAME: ${PACKAGE_NAME}
        BASE_IMAGE: ${BASE_IMAGE}