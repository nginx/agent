version: '3.9'

networks:
  monitoring:
    driver: bridge

services:
  agent:
    build: 
      context: ../../../
      dockerfile: ./scripts/docker/nginx-oss/ubuntu/Dockerfile
      args:
        - PACKAGE
        - DOCKER_IMAGE
    ports:
      - 9091:9091
    networks:
      - monitoring
      