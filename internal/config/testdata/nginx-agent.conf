log:
  level: debug
  path: ./

data_plane_api:
  host: 127.0.0.1
  port: 8038
process_monitor:
  monitoring_frequency: 30s

client: 
  timeout: 10s

config_dirs: "/etc/nginx:/usr/local/etc/nginx:/usr/share/nginx/modules"

metrics:
    produce_interval: "20s"
    otel_exporter:
        buffer_length: 20
        export_retry_count: 3
        export_interval: "20s"
        grpc:
            target: "http://localhost:4317"
            connection_timeout: "10s"
            minimum_connection_timeout: "5s"
            backoff_delay: "240s"
    prometheus_source:
        endpoints:
            - "http://localhost:9090"   
