default:
all_set:
  metrics:
    nginx.http.conn:
      enabled: true
    nginx.http.conn.count:
      enabled: true
    nginx.http.requests:
      enabled: true
    nginx.http.response.status:
      enabled: true
  resource_attributes:
    nginx.instance.id:
      enabled: true
    nginx.instance.type:
      enabled: true
    resource.id:
      enabled: true
none_set:
  metrics:
    nginx.http.conn:
      enabled: false
    nginx.http.conn.count:
      enabled: false
    nginx.http.requests:
      enabled: false
    nginx.http.response.status:
      enabled: false
  resource_attributes:
    nginx.instance.id:
      enabled: false
    nginx.instance.type:
      enabled: false
    resource.id:
      enabled: false
filter_set_include:
  resource_attributes:
    nginx.instance.id:
      enabled: true
      metrics_include:
        - regexp: ".*"
    nginx.instance.type:
      enabled: true
      metrics_include:
        - regexp: ".*"
    resource.id:
      enabled: true
      metrics_include:
        - regexp: ".*"
filter_set_exclude:
  resource_attributes:
    nginx.instance.id:
      enabled: true
      metrics_exclude:
        - strict: "nginx.instance.id-val"
    nginx.instance.type:
      enabled: true
      metrics_exclude:
        - strict: "nginx.instance.type-val"
    resource.id:
      enabled: true
      metrics_exclude:
        - strict: "resource.id-val"
