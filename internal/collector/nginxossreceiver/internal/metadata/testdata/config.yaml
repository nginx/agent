default:
all_set:
  metrics:
    nginx.http.conn:
      enabled: true
    nginx.http.conn.count:
      enabled: true
    nginx.http.requests:
      enabled: true
    nginx.http.response.status:
      enabled: true
none_set:
  metrics:
    nginx.http.conn:
      enabled: false
    nginx.http.conn.count:
      enabled: false
    nginx.http.requests:
      enabled: false
    nginx.http.response.status:
      enabled: false
