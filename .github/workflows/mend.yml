name: Mend

on:
  push:
    branches:
      - main
      - release-*
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+*"
    paths-ignore:
      - docs/**
  pull_request:
    branches:
      - main
      - release-*
    paths-ignore:
      - docs/**

concurrency:
  group: ${{ github.ref_name }}-mend
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  mend:
    if: >-
      ${{ 
        (github.event_name == 'pull_request' && !github.event.pull_request.head.repo.fork && !startsWith(github.head_ref, 'dependabot-') && !startsWith(github.head_ref, 'renovate/')) ||
        (github.event_name == 'push' && !github.event.repository.fork)
      }}
    uses: nginx/compliance-rules/.github/workflows/mend.yml@077e950021045cabd0d29b7d9e6d9cc01def6804 # v0.3.4
    secrets: inherit
    with:
      product_name: nginx-agent-v3_${{ github.ref_name }}
      project_name: nginx-agent-v3
