#
# /etc/nginx-agent/nginx-agent.conf
#
# Configuration file for NGINX Agent.
#

log:
  # set log level (error, info, debug; default "info")
  level: debug
  # set log path. if empty, don't log to file.
  path: /var/log/nginx-agent/

allowed_directories: 
    - /etc/nginx
    - /usr/local/etc/nginx
    - /usr/share/nginx/modules
    - /var/run/nginx
    - /var/log/nginx
    - /opt/homebrew/etc/nginx

# command server settings 
command:
  server:
    host: 127.0.0.1           # Command server host
    port: 9091                    # Command server port
    type: grpc                       # Server type (e.g., 0 for gRPC)
  

## collector metrics settings
# collector:
#   exporters:         # exporters
#     otlp_exporters:
#       - server:
#           host: "127.0.0.1"         # OTLP exporter server host
#           port: 5643                # OTLP exporter server port
#         tls: {}
